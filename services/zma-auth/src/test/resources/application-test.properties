# Test profile — defaults to H2 (PostgreSQL compatibility mode) when Docker is not present
spring.datasource.url=jdbc:h2:mem:zma_auth_db;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS auth;
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

jwt.secret=8tNA0cuicRf5NnUO0BTJhs7nCr2HrYtU/bgHvanwUiM=
jwt.expiration-ms=3600000

# Mock Google OAuth2 credentials for Spring Security test context
spring.security.oauth2.client.registration.google.client-id=test-google-client-id
spring.security.oauth2.client.registration.google.client-secret=test-google-client-secret
spring.security.oauth2.client.registration.google.scope=profile,email

mail.enabled=false
frontend.url=http://localhost:5173

users.service.url=http://localhost:65000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.default_schema=auth
spring.jpa.properties.hibernate.hbm2ddl.create_namespaces=true

management.health.mail.enabled=false
