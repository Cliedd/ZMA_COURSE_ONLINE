[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA : toutes les routes servent index.html (React Router côté client).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
