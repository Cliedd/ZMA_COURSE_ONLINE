[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA : toutes les routes servent index.html (React Router côté client).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
