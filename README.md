# ZMA Course Online

Monorepo: React frontend (Netlify) + Java microservices (Railway).

Backend services live under services/; deployed to Railway.

Ignore-command paths fixed 2026-08-02 (see netlify.toml).
